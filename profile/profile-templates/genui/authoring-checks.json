{
  "intentSpec": {
    "shapeValidators": [
      {
        "kind": "ajv-schema",
        "schema": {
          "type": "object",
          "properties": {
            "goal": { "type": "string" },
            "priorities": { "type": "array", "items": { "type": "string" } },
            "constraints": { "type": "array", "items": { "type": "string" } }
          }
        },
        "message": "Invalid intent"
      }
    ],
    "targetSteps": [
      { "expr": "priorities", "writeTo": "priorities" },
      { "expr": "constraints", "writeTo": "constraints" }
    ],
    "warning": {
      "code": "intent-target-unknown",
      "detailTemplate": "intent target '{name}' matches no facet of the interaction"
    }
  },
  "layerSemantics": [
    {
      "schemaFile": "schemas/presentation.schema.json",
      "kind": "facet-backed-layer",
      "steps": [
        { "expr": "source.interaction", "writeTo": "kind" },
        { "expr": "regions.name", "writeTo": "regionNames" }
      ],
      "unknownInteractionDetailTemplate": "source.interaction '{kind}' is not a known interaction kind",
      "missingRequiredDetailTemplate": "required facet '{facet}' is missing from the layer regions",
      "warnings": {
        "unknownRegion": {
          "code": "unknown-region",
          "detailTemplate": "region '{region}' does not correspond to a facet of '{kind}'"
        },
        "roleMismatch": {
          "code": "role-mismatch",
          "detailTemplate": "region '{region}' role '{role}' differs from taxonomy role '{facetRole}'"
        }
      }
    }
  ]
}
