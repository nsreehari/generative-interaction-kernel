{
  "layerPositions": {
    "source": {
      "label": "Source layer",
      "role": "The external input layer where the profile starts before any lowering rules run."
    },
    "intermediate": {
      "label": "Intermediate layer",
      "role": "An internal stage in the lowering pipeline that reshapes the previous layer into a more concrete representation."
    },
    "terminal": {
      "label": "Terminal layer",
      "role": "The final emitted layer after all lowering rules have run."
    },
    "unknown": {
      "label": "Pipeline stage",
      "role": "A stage in this profile's lowering pipeline."
    }
  },
  "stageLabelTemplate": "{label} · stage {index} of {total}",
  "sampleSeeds": [
    {
      "match": { "sourceKind": "workflow" },
      "kind": "workflow-rules",
      "slot": "interaction",
      "matchField": "workflow",
      "payloadField": "workflow",
      "labelPrefix": "workflow"
    },
    {
      "kind": "planning-match-values",
      "slot": "template",
      "matchField": "interaction",
      "payloadField": "interaction",
      "labelPrefix": "interaction"
    }
  ],
  "recipeViews": [
    {
      "match": { "family": "workflow" },
      "kind": "workflow-to-interaction",
      "kindLabel": "Workflow → Interaction",
      "tagline": "selects the interaction",
      "purpose": "Projects an upstream workflow state into the interaction that the rest of the profile will lower.",
      "regionRules": {
        "fromLabel": "When workflow is",
        "toLabel": "Selects interaction",
        "slots": ["interaction"],
        "from": { "kind": "path", "path": "match.workflow" },
        "to": { "path": "emit.interaction" }
      }
    },
    {
      "match": { "family": "planning" },
      "kind": "interaction-to-presentation",
      "kindLabel": "Interaction → Presentation",
      "tagline": "decides the layout",
      "purpose": "Picks a layout template for the interaction and orders each region by priority and disclosure for the target context.",
      "regionRules": {
        "fromLabel": "A region whose role is",
        "toLabel": "Is presented as",
        "slots": ["presentation"],
        "from": { "kind": "mapping-when" },
        "to": { "path": "emit.presentation" }
      }
    },
    {
      "match": { "family": "runtime" },
      "kind": "presentation-to-runtime",
      "kindLabel": "Presentation → Runtime",
      "tagline": "picks the components",
      "purpose": "Binds each presentation region to a concrete UI capability the kernel can render.",
      "containerCapability": { "slot": "container", "path": "emit.capability" },
      "regionRules": {
        "fromLabel": "A region / role",
        "toLabel": "Renders as",
        "slots": ["region"],
        "from": { "kind": "mapping-when" },
        "to": { "path": "emit.capability" }
      }
    }
  ]
}