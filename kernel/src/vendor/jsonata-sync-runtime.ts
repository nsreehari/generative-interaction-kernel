// @ts-ignore -- generated CommonJS sync port ships no type declarations.
import * as jsonataSyncModule from "./jsonata-sync.cjs";

const factory = (jsonataSyncModule as { default?: unknown }).default
  ?? (globalThis as typeof globalThis & { jsonata?: unknown }).jsonata;

export default factory;