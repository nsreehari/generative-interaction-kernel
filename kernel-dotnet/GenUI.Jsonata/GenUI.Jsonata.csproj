<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>GenUI.Jsonata</RootNamespace>
    <!-- Native C# port of the canonical JSONata language. Uses only System.Text.Json /
         System.Text.RegularExpressions from the shared framework: no package restore. -->
  </PropertyGroup>

</Project>
