{
  "gik": "0.1",
  "type": "vocabulary",
  "payload": {
    "version": "demo-runner/1.0",
    "expression": "jsonata",
    "namespaces": ["runner"],
    "contexts": ["demo", "control"],
    "actions": ["assign", "derive", "emit", "invoke"],
    "capabilities": {
      "demo:runner": {
        "propsSchema": { "type": "object", "additionalProperties": true },
        "slots": ["children"],
        "emits": ["reset", "finishAct"]
      },
      "demo:timer-button": {
        "propsSchema": { "type": "object", "additionalProperties": true },
        "emits": ["press"]
      },
      "fluent:toggle": {
        "propsSchema": { "type": "object", "additionalProperties": true },
        "emits": ["toggle"]
      },
      "fluent:dropdown": {
        "propsSchema": { "type": "object", "additionalProperties": true },
        "emits": ["select"]
      }
    },
    "externals": {
      "projectionViews": {
        "demo": { "from": "self" },
        "fluent": { "from": "fluent", "use": ["dropdown", "toggle"] }
      },
      "effectHandlers": ["requestNextAct", "setPace", "selectDemo", "setPresentationContext", "finishAct", "resetDemo"]
    }
  }
}
