{
  "gik": "0.1",
  "type": "lowering-recipe",
  "payload": {
    "id": "4layers.workflow-to-interaction",
    "from": "workflow",
    "to": "interaction",
    "metadata": { "executor": "select-interaction" },
    "program": [
      { "slot": "interaction", "match": { "workflow": "incident-triage" }, "emit": { "interaction": "investigate" } },
      { "slot": "interaction", "match": { "workflow": "portfolio-review" }, "emit": { "interaction": "review" } },
      { "slot": "interaction", "match": { "workflow": "operations-monitoring" }, "emit": { "interaction": "monitor" } },
      { "slot": "interaction", "match": { "workflow": "change-approval" }, "emit": { "interaction": "approve" } },
      { "slot": "interaction", "match": {}, "emit": { "interaction": "investigate" } }
    ]
  }
}