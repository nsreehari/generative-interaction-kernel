# @gik/blueprint-host

Compatibility package. Import `BlueprintHost` and `BlueprintHostProps` from `@gik/react` for new code.

```ts
export { BlueprintHost, type BlueprintHostProps } from "@gik/react";
```

This package adds no runtime behavior and will be removed after its compatibility window.