{
  "name": "negative: a malformed document is rejected (validate-before-commit)",
  "description": "An action missing the required `do` field fails structural validation; the kernel MUST refuse to construct.",
  "vocabulary": {
    "gik": "0.1",
    "type": "vocabulary",
    "payload": {
      "version": "bad-demo/1.0",
      "expression": "jsonata",
      "namespaces": ["card_data"],
      "capabilities": { "control": { "emits": ["tap"] } }
    }
  },
  "program": {
    "gik": "0.1",
    "type": "program",
    "payload": {
      "vocabulary": "bad-demo/1.0",
      "root": {
        "capability": "control",
        "id": "c",
        "edges": { "on": { "tap": [{ "target": "card_data.x" }] } }
      }
    }
  },
  "expectInvalid": true
}
