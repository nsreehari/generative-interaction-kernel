{
  "name": "genui-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GenUI Platform: GUP wire schemas, conformance fixtures, and the reference kernel.",
  "scripts": {
    "conformance": "node schemas/validate.mjs",
    "test:kernel": "node --import tsx --test kernel/test/**/*.test.ts",
    "test:react": "node --import tsx --test adapters/react/test/**/*.test.tsx",
    "test:sse": "node --import tsx --test transports/http-sse/test/**/*.test.ts",
    "typecheck": "tsc -p kernel/tsconfig.json --noEmit && tsc -p adapters/react/tsconfig.json --noEmit && tsc -p transports/http-sse/tsconfig.json --noEmit",
    "test": "npm run conformance && npm run typecheck && npm run test:kernel && npm run test:react && npm run test:sse"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "jsonata": "^2.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
