{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://genui.dev/gup/0.1/trace.schema.json",
  "title": "GUP trace message",
  "description": "An observability event emitted by the kernel to an ObservabilitySink.",
  "type": "object",
  "additionalProperties": false,
  "required": ["gup", "type", "payload"],
  "properties": {
    "gup": { "const": "0.1" },
    "type": { "const": "trace" },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event"],
      "properties": {
        "event": {
          "enum": ["resolve", "fallback", "action", "transition", "validate"]
        },
        "node": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_-]*$"
        },
        "detail": { "type": "object" },
        "ts": {
          "type": "number",
          "description": "Epoch milliseconds."
        }
      }
    }
  }
}
