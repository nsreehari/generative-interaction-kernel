<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>GenUI.AgentFace</RootNamespace>
    <!-- Uses only System.Text.Json (via GenUI.Kernel) from the shared framework: no package restore. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../../kernel-dotnet/GenUI.Kernel/GenUI.Kernel.csproj" />
  </ItemGroup>

</Project>
