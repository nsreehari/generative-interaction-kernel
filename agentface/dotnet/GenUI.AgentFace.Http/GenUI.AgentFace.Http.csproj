<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>GenUI.AgentFace.Http</RootNamespace>
    <!-- HTTP wrapper over the transport-free McpServer. Uses System.Net.HttpListener (BCL): no package restore. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../GenUI.AgentFace/GenUI.AgentFace.csproj" />
  </ItemGroup>

</Project>
